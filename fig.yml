mysql:
  image: mysql:latest
  environment:
    - MYSQL_ROOT_PASSWORD=mysupersecret
  volumes_from:
    - dungeon_db_data

redis:
  image: redis:latest

web:
  build: .
  volumes:
    - .:/dungeon
  ports:
    - "3000:3000"
  links:
    - mysql
    - redis
