
<% unless games.empty? %>

  <h3><%= title %></h3>
  <table>
    <thead>
      <tr>
        <th>Name</th>
      </tr>
    </thead>
    <% games.each do |g| %>
    <tr>
      <td><%= link_to g.name, game_path(g) %></td>
    </tr>
    <% end %>
  </table>

<% end %>